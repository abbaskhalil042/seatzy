export interface IAirplane {
  modelNumber: string;
  capacity: number;
}

export interface Icity {
  name: string;
}

export interface Iairport {
  name: string;
  code: string;
  address: string;
  cityId: number;
}
